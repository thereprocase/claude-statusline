#!/usr/bin/env python3
"""Edge-case tests — consolidated into test_statusline.py.

This file is intentionally empty. All tests that were here have been
rewritten and merged into test_statusline.py, which tests core.py
directly via import rather than through the shell dispatcher.

Run the full suite:
    python -m pytest test_statusline.py
    python test_statusline.py
"""
